services:
  feed:
    build: .
    ports:
      - "7291:7291"
    volumes:
      - feed-data:/app/data
    environment:
      - DB_PATH=/app/data/feed.db
      - ADDR=:7291
      - ADMIN_PASS=${ADMIN_PASS:-changeme}
    restart: unless-stopped

volumes:
  feed-data:
