services:
  feed:
    build: .
    ports:
      - "7291:7291"
    volumes:
      - feed-data:/app/data
    environment:
      - DB_PATH=/app/data/feed.db
      - ADDR=:7291
    restart: unless-stopped

volumes:
  feed-data:
